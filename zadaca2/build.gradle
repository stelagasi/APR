plugins {
    id 'java'
}

version 'unspecified'

repositories {
    mavenCentral()
}

dependencies {
    implementation(project(':zadaca1'))
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
