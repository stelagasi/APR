plugins {
    id 'java'
}

version 'unspecified'

repositories {
    mavenCentral()
}

dependencies {
    implementation(project(':zadaca1'))
    implementation(project(':zadaca2'))
    testCompile group: 'junit', name: 'junit', version: '4.12'
}
